<%- include("partials/header",{title: "Pokemon vangen geef ze een naam" , secondRow:true }); %>
    <main>
        <div class="container-fluid col-12 d-flex justify-content-center">
            <article id="successCard" class="card mb-3 col-12 col-sm6 d-flex justify-content-center" style="max-width: 450px;">
                <form class="row g-0" method="post">
                    <div class=".col md-4">
                        <img src="<%= Pokemon.image %>" class="img-fluid rounded-start" alt="<%= Pokemon.name %>">
                    </div>
                    <div class="">
                        <div class="card-body">
                            <h5 class="text-center">
                                Geef je nieuwe Pokémon een naam
                            </h5>
                            <input type="text" name="name" value="<%= Pokemon.name %>"
                                class="form-outline border border-0 border-bottom ms-2 text-center" style="width: 100%;" />
                        </div>
                    </div>
                    <input type="hidden" name="pokemon" value="<%= Pokemon.id %>">
                    <button type="submit" id="buttonW" formaction="/pokemonCatchSuccess?useDefault=true" class="btn btn-primary">Gebruik de naam "<%= Pokemon.name %>"</button>
                    <button type="submit" id="buttonW" class="btn btn-primary" formaction="/pokemonCatchSuccess?useDefault=false">Gebruik je bijnaam</button>
                </form>
            </article>
        </div>
    </main>

    <%- include("partials/footer"); %>